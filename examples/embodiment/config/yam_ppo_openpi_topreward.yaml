# YAM bimanual robot – PPO with TOPReward dense reward and π₀.5 (OpenPI) policy.
#
# Algorithm: PPO with GAE advantage, value head on VLM features, and dense
#            TOPReward progress signal computed via VLM True-token log-prob.
#
# TOPReward design:
#   The VLMPlannerWorker (Qwen3-VL-8B on Beaker) serves dual roles:
#     1. Subtask planning (optional, via subtask_interval).
#     2. Dense reward scoring — after each chunk step, a forward pass extracts
#        log p("True" | frames_1:t, instruction) as a progress score.
#        The reward delta r_t = score_t - score_{t-1} is injected into EnvOutput.
#
# Hardware topology (single Beaker node, 3 GPUs):
#   GPU 0 – actor (π₀.5 FSDP training with value head)
#   GPU 1 – rollout (π₀.5 inference)
#   GPU 2 – VLMPlannerWorker (Qwen3-VL-8B for TOPReward + subtask planning)
#   CPU   – EnvWorker (RemoteEnv gRPC client → desktop RobotServer)
#
# Desktop:
#   RobotServer wrapping YAMEnv, exposed via reverse SSH tunnel on :50051.
#   Start with: bash scripts/start_robot_server.sh --config .../yam_pi05_follower.yaml \
#                   --use-follower-servers --remote-host <beaker-tailscale-ip>
#
# Submit with:
#   bash scripts/submit_yam_training.sh --config yam_ppo_openpi_topreward \
#       --model-path thomas0829/folding_towel_pi05
#
# For TOPReward without VLM subtask planning, set subtask_interval: 0.
#
# Prerequisites:
#   pip install "transformers @ git+https://github.com/huggingface/transformers.git@main"
#   pip install qwen-vl-utils accelerate torchvision pillow  (on Beaker)
#   huggingface-cli download Qwen/Qwen3-VL-8B

defaults:
  - env/remote_yam@env.train
  - env/remote_yam@env.eval
  - model/pi0_5@actor.model
  - training_backend/fsdp@actor.fsdp_config
  - override hydra/job_logging: stdout

hydra:
  run:
    dir: .
  output_subdir: null
  searchpath:
    - file://${oc.env:EMBODIED_PATH}/config/

cluster:
  num_nodes: 1
  component_placement:
    actor:
      node_group: "gpu"
      placement: 0
    rollout:
      node_group: "gpu"
      placement: 1   # π₀.5 requires a dedicated GPU; cannot share GPU 0 with actor
    env:
      node_group: "gpu"
      placement: 0   # RemoteEnv runs CPU-only; placement on same node
  node_groups:
    - label: "gpu"
      node_ranks: 0
    - label: "beaker_vlm"
      node_ranks: 0  # VLMPlannerWorker placement group on the Beaker node

runner:
  task_type: embodied
  logger:
    log_path: "../results"
    project_name: rlinf
    experiment_name: "yam_ppo_openpi_topreward"
    logger_backends: ["tensorboard"]

  max_epochs: 2000
  max_steps: -1

  only_eval: False
  val_check_interval: -1
  save_interval: 50

  resume_dir: null
  ckpt_path: null

  weight_sync_interval: 1

algorithm:
  normalize_advantages: False
  kl_penalty: kl
  group_size: 1
  reward_coef: 1.0

  rollout_epoch: 1
  eval_rollout_epoch: 1

  reward_type: chunk_level
  logprob_type: chunk_level
  entropy_type: chunk_level

  update_epoch: 5
  adv_type: gae
  loss_type: actor_critic
  loss_agg_func: "token-mean"

  bootstrap_type: always
  kl_beta: 0.0    # NOTE: not used by EmbodiedFSDPActor (KL penalty only applies to reasoning tasks)
  entropy_bonus: 0.005
  clip_ratio_high: 0.2
  clip_ratio_low: 0.2
  clip_ratio_c: 3.0
  value_clip: 0.2
  huber_delta: 10.0

  gamma: 0.99
  gae_lambda: 0.95

  filter_rewards: False
  rewards_lower_bound: 0.0
  rewards_upper_bound: 1.0

  sampling_params:
    do_sample: True
    temperature_train: 1.0
    temperature_eval: 0.6
    top_k: 50
    top_p: 1.0
    repetition_penalty: 1.0
    add_BOS: False

  length_params:
    max_new_token: null
    max_length: 1024
    min_length: 1

env:
  group_name: "EnvGroup"
  # Optional local simulation of the robot desktop input path.
  # When enabled, train_embodied_agent_staged.py starts a local dummy
  # RobotServer automatically, so RemoteEnv still talks over gRPC but no
  # separate desktop machine or reverse SSH tunnel is required.
  #
  # Keep env.train.remote_server_url / env.eval.remote_server_url pointed at
  # localhost:<port> when using this mode.
  remote_desktop_simulation:
    enabled: False
    dummy: True
    env_config_path: null
    startup_timeout: 30.0

  train:
    total_num_envs: 1
    max_episode_steps: 100
    max_steps_per_rollout_epoch: 60
    control_rate_hz: 10.0

    # Task description must be a concrete episode goal, not a generic capability
    # label. Examples: "fold the towel", "pick up the red block and place it in
    # the bowl". Override via CLI: env.train.task_description="your task here"
    task_description: "fold the towel"

    # Enable TOPReward dense progress signal.
    top_reward_enabled: True
    top_reward_max_frames: 16
    # Anchor TOPReward to the episode-level goal so reward semantics are stable
    # across subtask changes. Use "current_task" only if stage-conditioned dense
    # reward is explicitly desired.
    top_reward_instruction_source: initial_task

    # Subtask update interval in CHUNK STEPS (0 to disable VLM planning).
    # With num_action_chunks=30 and max_steps_per_rollout_epoch=60,
    # n_train_chunk_steps=2. The EnvWorker resets the subtask counter at the
    # start of each rollout epoch (bootstrap_step), so subtask_interval must
    # be <= n_train_chunk_steps to fire within an episode.
    # 1 chunk step = 30 env steps = subtask update at ~50% and ~100% of episode.
    subtask_interval: 1

    # Dummy mode: controlled at the ROBOT SERVER, not here.
    # RemoteEnv does not read is_dummy — it proxies calls over gRPC to the server.
    # To test without real hardware, start the robot server with --dummy:
    #   bash scripts/start_robot_server.sh --config .../yam_pi05_follower.yaml --dummy

  eval:
    total_num_envs: 1
    max_episode_steps: 100
    max_steps_per_rollout_epoch: 60
    control_rate_hz: 10.0
    task_description: "fold the towel"

# VLM planner: Qwen3-VL-8B for both subtask planning and TOPReward scoring.
# reward_scale is read from THIS section by TOPReward (not from reward.reward_scale).
vlm_planner:
  model_path: "Qwen/Qwen3-VL-8B-Instruct"
  backend: "transformers"
  dtype: "bfloat16"
  transformers_runtime_path: ".qwen3_runtime"
  # Optional explicit placement override. By default RLinf chooses GPU 2 here
  # because actor and rollout occupy GPUs 0 and 1 on the same node.
  # placement: 2
  # Optional node-group override for the planner. Defaults to "beaker_vlm".
  # node_group: "beaker_vlm"
  max_new_tokens_subtask: 64
  max_new_tokens_reward: 16
  success_threshold: 0.5
  top_reward_enabled: True
  top_reward_max_frames: 16
  reward_scale: 1.0  # multiplied onto each log-prob score before delta computation

rollout:
  group_name: "RolloutGroup"
  generation_backend: "huggingface"
  recompute_logprobs: False
  unnorm_key: libero_10
  enable_offload: False
  pipeline_stage_num: 1

  collect_transitions: True
  collect_prev_infos: True  # required for adv_type: gae — prev_values must be collected for GAE bootstrap

  model:
    model_path: "thomas0829/folding_towel_pi05"
    precision: ${actor.model.precision}

  enable_torch_compile: False
  enable_cuda_graph: False

actor:
  group_name: "ActorGroup"
  training_backend: "fsdp"

  # rollout_size = n_chunk_steps * total_num_envs * rollout_epoch = 2 * 1 * 1 = 2
  # global_batch_size must divide rollout_size; micro_batch_size must divide global_batch_size.
  micro_batch_size: 2
  global_batch_size: 2
  seed: 42
  enable_offload: False

  model:
    model_path: "thomas0829/folding_towel_pi05"
    model_type: "openpi"
    add_value_head: True
    num_action_chunks: 30
    action_dim: 14   # YAM bimanual: 2 × 7 DOF; sets openpi.action_env_dim via interpolation
    num_steps: 10
    openpi:
      value_after_vlm: True
      value_vlm_mode: mean_token
      config_name: "pi05_yam_follower"
      num_images_in_input: 3
      discrete_state_input: True
      noise_method: "flow_sde"
      action_horizon: 30
      noise_params: [0.16, 0.12, 200]
      joint_logprob: True

  optim:
    lr: 5.0e-6
    value_lr: 1.55e-4
    adam_beta1: 0.9
    adam_beta2: 0.95
    adam_eps: 1.0e-08
    clip_grad: 1.0
    weight_decay: 0.01
    lr_scheduler: torch_constant
    critic_warmup_steps: 0

  fsdp_config:
    strategy: "fsdp"
    sharding_strategy: "no_shard"
    gradient_checkpointing: False
    mixed_precision:
      param_dtype: ${actor.model.precision}
      reduce_dtype: ${actor.model.precision}
      buffer_dtype: ${actor.model.precision}

# reward section is metadata only when use_reward_model: False.
# TOPReward is computed inline by VLMPlannerWorker (see vlm_planner.reward_scale above).
# reward_scale here is NOT read by TOPReward — configure it under vlm_planner instead.
reward:
  use_reward_model: False
  reward_type: top_reward

critic:
  use_critic_model: False
